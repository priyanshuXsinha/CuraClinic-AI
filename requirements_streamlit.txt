# Core ML stack
torch>=2.1.0
transformers==4.45.2
trl==0.14.0
peft==0.14.0
xformers==0.0.28.post3
accelerate>=0.26.0

# Unsloth (GPU-optimised fine-tuning + inference)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Dataset
datasets>=2.18.0

# HF Hub
huggingface_hub>=0.22.0

# Streamlit
streamlit>=1.33.0
